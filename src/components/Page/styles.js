import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  background: {
    display: 'flex',
    flex: 1,
    width: '100%',
    height: '100%',
    overflow: 'scroll',
  },
});

export default styles;
