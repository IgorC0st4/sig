import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  centerAlign: {
    alignContent: 'center',
    alignItems: 'center',
    padding: 10,
    height: '100%',
    width: '100%',
  },
  fullWidth: {
    width: '100%',
  },
  fab: {
    position: 'absolute',
    margin: 16,
    right: 0,
    bottom: 0,
  },
});

export default styles;
